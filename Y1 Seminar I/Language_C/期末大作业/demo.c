#include <stdio.h>
#include <math.h>
#define pi 3.14159

int main()
{
   double x;
   scanf("%lf",&x);
   if (x <0)
   {
      /* code */
      printf("The number is error");

   }else {
      printf("%lf",sqrt(x));
   }

}